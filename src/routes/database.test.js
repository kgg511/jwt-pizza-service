const { db_class } = require('../database/database.js');

const request = require('supertest');
const app = require('../service');

test("clear db", async()=>{

    // db = new db_class();
    // db.clearDB();
    // db.initializeDatabase();

})